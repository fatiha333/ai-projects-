# AI Agent Instructions for week1

## Project overview
- This workspace contains two Python practice scripts: `day1.py` and `day2.py`.
- It appears to be a small learning or homework repository rather than a production application.
- There is no package manager, tests, or additional documentation in this folder.

## Recommended agent behavior
- Treat this as a Python scripting exercise.
- Do not assume any framework, web server, or dependency management system.
- Use the current Python interpreter (likely `python3` / `python3.12`) when running code.
- Prefer simple, explicit fixes and improvements over broad refactors.

## How to run
- Run scripts directly:
  - `python3 day1.py`
  - `python3 day2.py`

## Notes for code assistance
- `day2.py` is an income-tax calculator exercise and includes inline problem requirements.
- Expect user-facing input prompts and print output, not imported library behavior.
- Keep changes minimal and preserve the learning intent of the scripts.
